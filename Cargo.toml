[package]
name = "anodos"
version = "1.3.0"
authors = ["Tom Cant"]
edition = "2024"
build = "build/main.rs"

[dependencies]
smallvec = "1.15.1"

[profile.release]
codegen-units = 1
lto = true
panic = "abort"
strip = true
